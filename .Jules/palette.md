## 2024-05-23 - Icon-Only Buttons
**Learning:** Icon-only buttons (like search or cart) must have both an `aria-label` for screen readers and a tooltip for mouse users.
**Action:** Always wrap icon buttons in `Tooltip` components and add descriptive `aria-label` attributes.
