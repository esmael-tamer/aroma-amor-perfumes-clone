## 2026-01-14 - Tooltips for Icon-Only Buttons
**Learning:** Icon-only buttons (like "Add to Cart") provide limited context for visual users. While `aria-label` supports screen readers, adding a tooltip ensures all users understand the action before clicking.
**Action:** Wrap icon-only buttons with the `Tooltip` component to improve clarity and confidence in user interactions.
